# Jekyll configuration
title: Yearly for iOS
description: Support site for Yearly iOS app

# Build settings
markdown: kramdown
permalink: pretty

# Keep faq.json in output (don't process it)
keep_files:
  - faq.json

# Exclude files from processing
exclude:
  - README.md
  - .gitignore

